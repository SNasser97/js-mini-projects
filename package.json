{
  "name": "projects",
  "version": "1.0.0",
  "description": "Mini js projects",
  "main": "index.js",
  "scripts": {
    "test": "jest --watchAll *test.js",
    "start": "parcel -p 3000 watch src/index.html --out-dir dev --no-cache",
    "build": "parcel build src/index.html --no-cache"
  },
  "author": "Nasser",
  "license": "ISC",
  "devDependencies": {
    "jest": "^26.6.1",
    "node-sass": "4.14.1",
    "sass": "1.27.0"
  },
  "dependencies": {
    "parcel-bundler": "^1.12.4"
  }
}
